"""There is intentionally no default document config.

This file is only a pointer for people who expect a root ``config.py``.
It will not work as a parser config. Select the appropriate config explicitly
from ``examples/`` instead, for example:

    python parse.py input.pdf --config examples/seje-1957/config.py -o out.xml
"""
